import React from 'react';
import classes from '../../styles/components/TopBar/TopBar.module.scss';

const TopBar = () => {
    return <header className={classes.container}>Hi there</header>;
};

export default TopBar;
